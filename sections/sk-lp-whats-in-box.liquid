{%- comment -%}
  sk-lp-whats-in-box.liquid
  "What's in the box" section with image and dynamic item list.
{%- endcomment -%}

{{ 'section-sk-lp-radiance-face.css' | asset_url | stylesheet_tag }}

<div class="sk-lp">
  <section class="lp-whats-in-box">
    <div class="lp-container">
      <div class="lp-whats-in-box__grid">
        <div class="lp-whats-in-box__image-col">
          {%- if section.settings.image != blank -%}
            <img
              src="{{ section.settings.image | image_url: width: 800 }}"
              srcset="{{ section.settings.image | image_url: width: 400 }} 400w, {{ section.settings.image | image_url: width: 600 }} 600w, {{ section.settings.image | image_url: width: 800 }} 800w"
              sizes="(max-width: 991px) 100vw, 50vw"
              alt="{{ section.settings.image.alt | default: 'Hva du får i esken' }}"
              width="{{ section.settings.image.width }}"
              height="{{ section.settings.image.height }}"
              loading="lazy"
            >
          {%- else -%}
            <div class="lp-whats-in-box__image-placeholder">[ Bilde: Unboxing / innhold i esken ]</div>
          {%- endif -%}
        </div>

        <div class="lp-whats-in-box__content">
          <h2 class="lp-section-heading">{{ section.settings.heading }}</h2>

          {%- if section.blocks.size > 0 -%}
            <ul class="lp-whats-in-box__list">
              {%- for block in section.blocks -%}
                <li class="lp-whats-in-box__item" {{ block.shopify_attributes }}>
                  <span class="lp-whats-in-box__item-icon">{{ block.settings.icon }}</span>
                  {{ block.settings.text }}
                </li>
              {%- endfor -%}
            </ul>
          {%- endif -%}
        </div>
      </div>
    </div>
  </section>
</div>

{% schema %}
{
  "name": "LP What's In The Box",
  "tag": "section",
  "class": "sk-lp-section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Overskrift",
      "default": "Hva får du i esken?"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Unboxing-bilde"
    }
  ],
  "blocks": [
    {
      "type": "item",
      "name": "Innhold",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Ikon (emoji)",
          "default": "💡"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Tekst",
          "default": "Skinora® LED-maske"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "LP What's In The Box",
      "blocks": [
        { "type": "item", "settings": { "icon": "💡", "text": "Skinora® Radiance Face LED-maske" } },
        { "type": "item", "settings": { "icon": "🔌", "text": "USB-C ladekabel" } },
        { "type": "item", "settings": { "icon": "🕶️", "text": "Øyebeskyttelse" } },
        { "type": "item", "settings": { "icon": "📖", "text": "Bruksanvisning (norsk)" } },
        { "type": "item", "settings": { "icon": "🎁", "text": "Premium oppbevaringspose" } }
      ]
    }
  ]
}
{% endschema %}
