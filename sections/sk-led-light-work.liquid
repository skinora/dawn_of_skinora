<style>
  /* ─────────────────────────────────────────────
   SKINORA – CLEAN CLINICAL 3-COLUMN DESKTOP
   MOBILE KEEPS ORIGINAL SLIDER
────────────────────────────────────────────── */

  .sk-wavelengths {
    padding: 4rem 0;
  }

  .sk-wavelengths__content {
    text-align: center;
    margin-bottom: 2.5rem;
  }

  .sk-wavelengths__heading {
    font-size: clamp(32px, 3vw, 48px);
    font-weight: 600;
    color: #30273e;
    margin: 0;
    line-height: 1.1;
  }

  /* ---------- Desktop Grid (3 Column) ---------- */
  .sk-wavelengths__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 48px;
    max-width: 1300px;
    margin: 0 auto;
    padding: 0 20px;
  }

  .sk-wavelength {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 1.75rem;
  }

  /* Image */
  .sk-wavelength__image-wrapper {
    width: 100%;
    max-width: 360px;
    margin: 0 auto;
  }

  .sk-wavelength__image-wrapper img {
    width: 100%;
    height: auto;
    display: block;
  }

  /* Title */
  .sk-wavelength__title {
    font-size: 22px;
    font-weight: 600;
    color: #30273e;
    margin: 0;
  }

  /* Benefits */
  .sk-wavelength__benefits {
    list-style: none;
    margin: 0;
    padding: 0;
    text-align: left;
    max-width: 320px;
    font-size: 16px;
    line-height: 1.55;
    color: #333;
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }

  .sk-wavelength__benefits li {
    position: relative;
    padding-left: 20px;
  }

  .sk-wavelength__benefits li::before {
    content: '•';
    position: absolute;
    top: 0;
    left: 0;
    font-size: 20px;
    color: #30273e;
  }

  /* Desktop only */
  @media (max-width: 750px) {
    .sk-wavelengths__grid {
      display: none;
    }
  }

  /* Mobile only */
  @media (min-width: 751px) {
    .sk-wavelengths__slider {
      display: none !important;
    }
  }
</style>

<section class="sk-wavelengths section--full-bleed">
  <div class="page-width">
    {% if section.settings.sk_led_light_work_heading %}
      <div class="sk-wavelengths__content">
        <h2 class="sk-wavelengths__heading">
          {{ section.settings.sk_led_light_work_heading | escape }}
        </h2>
      </div>
    {% endif %}

    {% assign visible_blocks = section.blocks | where_exp: 'block', 'block.settings.sk_wavelength_visible' %}
    {% assign count = visible_blocks.size %}

    {%- comment -%}
      ============= DESKTOP 3-COLUMN GRID =============
    {%- endcomment -%}

    {% if count > 0 %}
      <div class="sk-wavelengths__grid">
        {% for block in visible_blocks %}
          <div class="sk-wavelength">
            <div class="sk-wavelength__image-wrapper">
              <img
                src="{{ block.settings.sk_wavelength_image | image_url: width: 600 }}"
                alt="{{ block.settings.sk_wavelength_heading | escape }}"
                loading="lazy"
                width="{{ block.settings.sk_wavelength_image.width }}"
                height="{{ block.settings.sk_wavelength_image.height }}"
              >
            </div>

            <h3 class="sk-wavelength__title">{{ block.settings.sk_wavelength_heading }}</h3>

            <ul class="sk-wavelength__benefits">
              {% assign items = block.settings.sk_wavelength_benefits | split: '\n' %}
              {% for item in items %}
                {% assign trimmed = item | strip %}
                {% if trimmed != blank %}
                  <li>{{ trimmed }}</li>
                {% endif %}
              {% endfor %}
            </ul>
          </div>
        {% endfor %}
      </div>
    {% endif %}

    {%- comment -%}
      ============= MOBILE SWIPER =============
      (Your original slider preserved untouched)
    {%- endcomment -%}

    {% if count > 0 %}
      <div class="sk-wavelengths__slider sk-wavelengths-swiper-{{ section.id }}">
        <div class="swiper llw-swiper-{{ section.id }}">
          <div class="swiper-wrapper">
            {% for block in visible_blocks %}
              <div class="swiper-slide">
                <div class="sk-wavelength">
                  <div class="sk-wavelength__image-wrapper">
                    <img
                      src="{{ block.settings.sk_wavelength_image | image_url: width: 600 }}"
                      alt="{{ block.settings.sk_wavelength_heading | escape }}"
                      loading="lazy"
                      width="{{ block.settings.sk_wavelength_image.width }}"
                      height="{{ block.settings.sk_wavelength_image.height }}"
                    >
                  </div>

                  <h3 class="sk-wavelength__title">{{ block.settings.sk_wavelength_heading }}</h3>

                  <ul class="sk-wavelength__benefits">
                    {% assign items = block.settings.sk_wavelength_benefits | split: '\n' %}
                    {% for item in items %}
                      {% assign trimmed = item | strip %}
                      {% if trimmed != blank %}
                        <li>{{ trimmed }}</li>
                      {% endif %}
                    {% endfor %}
                  </ul>
                </div>
              </div>
            {% endfor %}
          </div>

          <div class="swiper-pagination"></div>
        </div>
      </div>
    {% endif %}
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    var mql = window.matchMedia('(max-width: 750px)');
    var swiperInstance = null;
    var container = document.querySelector('.sw-{{ section.id }}');

    function setup() {
      if (mql.matches && !swiperInstance) {
        swiperInstance = new Swiper(container, {
          slidesPerView: 1,
          spaceBetween: 20,
          pagination: {
            el: container.querySelector('.swiper-pagination'),
            clickable: true,
          },
        });
      } else if (!mql.matches && swiperInstance) {
        swiperInstance.destroy(true, true);
        swiperInstance = null;
      }
    }

    setup();
    mql.addEventListener('change', setup);
  });
</script>

{% schema %}
{
  "name": "SK Led Light Work",
  "settings": [
    {
      "type": "text",
      "id": "sk_led_light_work_heading",
      "label": "Title",
      "default": "Hvordan Fungerer Skinora LED Lys"
    }
  ],
  "blocks": [
    {
      "type": "wavelength",
      "name": "Wavelength",
      "limit": 4,
      "settings": [
        {
          "type": "image_picker",
          "id": "sk_wavelength_image",
          "label": "Image"
        },
        {
          "type": "checkbox",
          "id": "sk_wavelength_visible",
          "label": "Display block",
          "default": true
        },
        {
          "type": "text",
          "id": "sk_wavelength_heading",
          "label": "Heading",
          "default": "630 nm Rødt Lys"
        },
        {
          "type": "textarea",
          "id": "sk_wavelength_benefits",
          "label": "Benefits",
          "default": "Stimulerer kollagen\nForbedrer glød\nFremmer redusert fine linjer"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "SK Led Light Work",
      "blocks": [
        {
          "type": "wavelength",
          "settings": {
            "sk_wavelength_heading": "630 nm Rødt Lys",
            "sk_wavelength_benefits": "Stimulerer kollagen\nForbedrer glød\nFremmer redusert fine linjer"
          }
        },
        {
          "type": "wavelength",
          "settings": {
            "sk_wavelength_heading": "830 nm Nært infrarødt",
            "sk_wavelength_benefits": "Øker næring til celler\nReduserer hevelse\nStøtter dypere penetrasjon"
          }
        },
        {
          "type": "wavelength",
          "settings": {
            "sk_wavelength_heading": "1072 nm Dyp infrarød",
            "sk_wavelength_benefits": "Penetrerer dypere vev\nBidrar til avslapning\nStøtter cellefornyelse"
          }
        }
      ]
    }
  ]
}
{% endschema %}
