{% comment %}theme-check-disable UndefinedObject{% endcomment %}
{{ 'section-footer.css' | asset_url | stylesheet_tag }}
{{ 'component-newsletter.css' | asset_url | stylesheet_tag }}
{{ 'component-list-menu.css' | asset_url | stylesheet_tag }}
{{ 'component-list-payment.css' | asset_url | stylesheet_tag }}
{{ 'component-list-social.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {


    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<footer class="footer color-{{ section.settings.color_scheme }} gradient section-{{ section.id }}-padding">
  {%- liquid
    assign has_social_icons = true
    if settings.social_facebook_link == blank and settings.social_instagram_link == blank and settings.social_youtube_link == blank and settings.social_tiktok_link == blank and settings.social_twitter_link == blank and settings.social_pinterest_link == blank and settings.social_tumblr_link == blank and settings.social_vimeo_link == blank
      assign has_social_icons = false
    endif
  -%}
  <section class="footer-top_section">
    <div class="footer-top_section-content">
      <h1 class="sk-section-heading sk-section-heading--h1">
        {{ section.settings.newsletter_top_heading | default: 'Join our newsletter' }}
      </h1>
      {% if section.settings.newsletter_top_subtext != blank %}
        <p class="newsletter-subtext">{{ section.settings.newsletter_top_subtext }}</p>
      {% endif %}
      {%- form 'customer', id: 'ContactFooterTop', class: 'footer__newsletter-form newsletter-form' -%}
        <input type="hidden" name="contact[tags]" value="newsletter">
        <div class="newsletter-form__field-wrapper">
          <div class="field">
            <input
              id="NewsletterForm--{{ section.id }}"
              type="email"
              name="contact[email]"
              class="field__input"
              value="{{ form.email }}"
              aria-required="true"
              autocorrect="off"
              autocapitalize="off"
              autocomplete="email"
              {% if form.errors %}
                autofocus
                aria-invalid="true"
                aria-describedby="ContactFooterTop-error"
              {% elsif form.posted_successfully? %}
                aria-describedby="ContactFooterTop-success"
              {% endif %}
              placeholder="{{ section.settings.newsletter_placeholder | default: 'Email' }}"
              required
            >
            <button
              type="submit"
              class="newsletter-form__button field__button footer_submit_btn"
              name="commit"
              id="Subscribe"
              aria-label="{{ 'newsletter.button_label' | t }}"
            >
              <span class="news_letter_join-btn">{{ section.settings.newsletter_top_cta | default: 'Join' }}</span>
              {% comment %}
                <span class="svg-wrapper">
                  {{- 'icon-arrow.svg' | inline_asset_content -}}
                </span>
              {% endcomment %}
            </button>
          </div>
          {%- if form.errors -%}
            <small class="newsletter-form__message form__message" id="ContactFooterTop-error">
              <span class="svg-wrapper">
                {{- 'icon-error.svg' | inline_asset_content -}}
              </span>
              {{- form.errors.translated_fields.email | capitalize }}
              {{ form.errors.messages.email -}}
            </small>
          {%- endif -%}
          {%- if form.posted_successfully? -%}
            <p
              class="newsletter-form__message newsletter-form__message--success form__message"
              id="ContactFooterTop-success"
              tabindex="-1"
              autofocus
            >
              <span class="svg-wrapper">
                {{- 'icon-success.svg' | inline_asset_content -}}
              </span>
              {{- 'newsletter.success' | t }}
            </p>
          {%- endif -%}
        </div>
      {%- endform -%}
    </div>
  </section>
  <div class="footer__content-top container">
    <div class="footer__main-columns{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
      {%- comment -%} First Column: Logo, Description, Newsletter, Social Icons {%- endcomment -%}
      <div class="footer__column footer__column--brand">
        <div class="footer__logo">
          <a href="{{ routes.root_url }}" class="footer__logo-link" aria-label="{{ shop.name | escape }}">
            {{ 'custom-logo.svg' | inline_asset_content }}
            <span class="visually-hidden">{{ shop.name }}</span>
          </a>
        </div>

        {%- if section.settings.show_brand_description and section.settings.brand_description != blank -%}
          <div class="footer__brand-description">
            {{ section.settings.brand_description }}
          </div>
        {%- endif -%}

        {%- if section.settings.show_social and has_social_icons -%}
          {%- render 'social-icons', class: 'footer__social-icons' -%}
        {%- endif -%}
      </div>

      {%- comment -%} Three Menu Columns {%- endcomment -%}
      {%- if section.settings.footer_menu != blank -%}
        {%- assign footer_menu = linklists[section.settings.footer_menu] -%}
        {%- for parent_link in footer_menu.links limit: 3 -%}
          <div class="footer__column footer__column--menu">
            <h3 class="footer__menu-heading">{{ parent_link.title | escape }}</h3>
            {%- if parent_link.links != blank -%}
              <ul class="footer__menu-list list-unstyled">
                {%- for child_link in parent_link.links -%}
                  <li class="footer__menu-item">
                    <a href="{{ child_link.url }}" class="footer__menu-link">
                      {{ child_link.title | escape }}
                    </a>
                  </li>
                {%- endfor -%}
              </ul>
            {%- endif -%}
          </div>
        {%- endfor -%}
      {%- endif -%}
    </div>
  </div>

  <div
    class="footer__content-bottom{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}"
    {% if settings.animations_reveal_on_scroll %}
      data-cascade
    {% endif %}
  >
    <div class="footer__content-bottom-wrapper container">
      <div class="footer__column footer__localization isolate">
        {%- if section.settings.enable_country_selector and localization.available_countries.size > 1 -%}
          <localization-form>
            {%- form 'localization', id: 'FooterCountryForm', class: 'localization-form' -%}
              <div>
                <h2 class="caption-large text-body" id="FooterCountryLabel">{{ 'localization.country_label' | t }}</h2>
                {%- render 'country-localization', localPosition: 'FooterCountry' -%}
              </div>
            {%- endform -%}
          </localization-form>
        {%- endif -%}

        {%- if section.settings.enable_language_selector and localization.available_languages.size > 1 -%}
          <localization-form>
            {%- form 'localization', id: 'FooterLanguageForm', class: 'localization-form' -%}
              <div>
                <h2 class="caption-large text-body" id="FooterLanguageLabel">
                  {{ 'localization.language_label' | t }}
                </h2>
                {%- render 'language-localization', localPosition: 'FooterLanguage' -%}
              </div>
            {%- endform -%}
          </localization-form>
        {%- endif -%}
      </div>
      <div class="footer__column footer__column--info">
        <div class="footer__payment-shipping-wrapper">
          {%- if section.settings.payment_enable -%}
            <div class="footer__payment">
              <div>
                <h5>{{ 'sections.footer.payment' | t }}</h5>
              </div>

              {% if shop.enabled_payment_types.size > 0 %}
                {%- assign excluded_payment_types = 'american_express,unionpay' | split: ',' -%}
                <ul class="list list-payment" role="list">
                  {% for type in shop.enabled_payment_types %}
                    {%- unless excluded_payment_types contains type -%}
                      <li class="list-payment__item">
                        {{ type | payment_type_svg_tag }}
                      </li>
                    {%- endunless -%}
                  {% endfor %}
                  {%- comment -%} Vipps - Third-party payment gateway not in Shopify's native list {%- endcomment -%}
                  {% if settings.show_vipps_payment_icon %}
                    <li class="list-payment__item">
                      <img src="{{ 'icon-vipps.svg' | asset_url }}" alt="Vipps" height="22" width="55" loading="lazy">
                    </li>
                  {% endif %}
                </ul>
              {% endif %}
            </div>
          {%- endif -%}
        </div>
      </div>
    </div>
    <div class="footer__content-bottom-wrapper container{% if section.settings.enable_country_selector == false and section.settings.enable_language_selector == false %} footer__content-bottom-wrapper--center{% endif %}">
      <div class="footer__copyright caption">
        <small class="copyright__content">
          {{ section.settings.copyright_text | default: 'Skinora v/Providento AS - 933 648 672 MVA' }} &copy;
          {{ 'now' | date: '%Y' }}
        </small>
        {% comment %} <small class="copyright__content">{{ powered_by_link }}</small> {% endcomment %}
        {%- if section.settings.show_policy -%}
          <ul class="policies list-unstyled">
            {%- for policy in shop.policies -%}
              {%- if policy != blank -%}
                <li>
                  <small class="copyright__content">
                    <a href="{{ policy.url }}">{{ policy.title | escape }}</a>
                  </small>
                </li>
              {%- endif -%}
            {%- endfor -%}
          </ul>
        {%- endif -%}
      </div>
    </div>
  </div>
  {%- if section.settings.enable_org_schema -%}
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "Organization",
        "name": {{ shop.name | json }},
        "url": {{ shop.url | json }},
        "logo": {{ 'custom-logo.svg' | asset_url | prepend: "https:" | json }},
        "description": {{ section.settings.brand_description | strip_html | strip | json }},
        "sameAs": [{% assign socials = '' -%}
          {%- if settings.social_instagram_link != blank -%}{% assign socials = socials | append: settings.social_instagram_link | append: '|' %}{%- endif -%}
          {%- if settings.social_facebook_link != blank -%}{% assign socials = socials | append: settings.social_facebook_link | append: '|' %}{%- endif -%}
          {%- if settings.social_youtube_link != blank -%}{% assign socials = socials | append: settings.social_youtube_link | append: '|' %}{%- endif -%}
          {%- if settings.social_tiktok_link != blank -%}{% assign socials = socials | append: settings.social_tiktok_link | append: '|' %}{%- endif -%}
          {%- if settings.social_twitter_link != blank -%}{% assign socials = socials | append: settings.social_twitter_link | append: '|' %}{%- endif -%}
          {%- if settings.social_pinterest_link != blank -%}{% assign socials = socials | append: settings.social_pinterest_link | append: '|' %}{%- endif -%}
          {%- assign sameas = socials | split: '|' | reject: '' -%}
          {%- for u in sameas -%}{{ u | json }}{% unless forloop.last %}, {% endunless %}{%- endfor -%}
        ],
        "contactPoint": [{
          "@type": "ContactPoint",
          "contactType": "customer support",
          "email": {{ shop.email | json }},
          "areaServed": "NO"
        }]
      }
    </script>
  {%- endif -%}
</footer>

{% schema %}
{
  "name": "t:sections.footer.name",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Brand Information"
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo"
    },
    {
      "type": "range",
      "id": "logo_width",
      "min": 50,
      "max": 300,
      "step": 10,
      "unit": "px",
      "label": "Logo width",
      "default": 120
    },
    {
      "type": "checkbox",
      "id": "show_brand_description",
      "label": "Show brand description in footer",
      "default": false
    },
    {
      "type": "richtext",
      "id": "brand_description",
      "label": "Brand description",
      "default": "<p>Sign up to be the first to know about new expert product launches, the latest beauty trends and more.</p>"
    },
    {
      "type": "header",
      "content": "Newsletter"
    },
    {
      "type": "checkbox",
      "id": "newsletter_enable",
      "default": true,
      "label": "Enable newsletter signup"
    },
    {
      "type": "inline_richtext",
      "id": "newsletter_heading",
      "default": "SUBSCRIBE",
      "label": "Newsletter heading"
    },
    {
      "type": "text",
      "id": "newsletter_placeholder",
      "default": "Email",
      "label": "Email placeholder text"
    },
    {
      "type": "header",
      "content": "Menu"
    },
    {
      "type": "link_list",
      "id": "footer_menu",
      "label": "Footer menu",
      "info": "Select the menu with nested structure (parent -> children). Only the first 3 parent items will be displayed as columns."
    },
    {
      "type": "header",
      "content": "Social Media"
    },
    {
      "type": "checkbox",
      "id": "show_social",
      "default": true,
      "label": "Show social media icons",
      "info": "Configure social media links in theme settings"
    },
    {
      "type": "header",
      "content": "Follow on Shop"
    },
    {
      "type": "checkbox",
      "id": "enable_follow_on_shop",
      "default": true,
      "label": "Enable Follow on Shop",
      "info": "Display Follow on Shop button to allow customers to follow your store"
    },
    {
      "type": "header",
      "content": "Localization"
    },
    {
      "type": "checkbox",
      "id": "enable_country_selector",
      "default": true,
      "label": "Enable country/region selector",
      "info": "To add a country/region, go to your [payment settings.](/admin/settings/payments)"
    },
    {
      "type": "checkbox",
      "id": "enable_language_selector",
      "default": true,
      "label": "Enable language selector",
      "info": "To add a language, go to your [language settings.](/admin/settings/languages)"
    },
    {
      "type": "header",
      "content": "Payment Icons"
    },
    {
      "type": "checkbox",
      "id": "payment_enable",
      "default": true,
      "label": "Show payment icons"
    },
    {
      "type": "header",
      "content": "Policies"
    },
    {
      "type": "checkbox",
      "id": "show_policy",
      "default": true,
      "label": "Show policy links",
      "info": "To add or edit policies, go to your [policy settings.](/admin/settings/legal)"
    },
    {
      "type": "header",
      "content": "t:sections.all.spacing"
    },
    {
      "type": "range",
      "id": "margin_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.footer.settings.margin_top.label",
      "default": 0
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    },
    {
      "type": "text",
      "id": "newsletter_top_heading",
      "label": "Top newsletter heading",
      "default": "Join our newsletter"
    },
    { "type": "text", "id": "newsletter_top_cta", "label": "Top newsletter button label", "default": "Join" },
    {
      "type": "text",
      "id": "copyright_text",
      "label": "Copyright text",
      "default": "Skinora v/Providento AS - 933 648 672 MVA"
    },
    { "type": "checkbox", "id": "enable_org_schema", "label": "Enable Organization schema (JSON-LD)", "default": true },
    {
      "type": "inline_richtext",
      "id": "newsletter_top_subtext",
      "label": "Top newsletter subtext",
      "default": "Få inspirasjon, velvære-tips og eksklusive tilbud rett i innboksen. Vi deler kun det som får huden – og deg – til å stråle."
    }
  ]
}
{% endschema %}
