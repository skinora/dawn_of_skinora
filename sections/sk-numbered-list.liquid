{{ 'section-sk-numbered-list.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  }
{%- endstyle -%}

<div class="sk-numbered-list section-{{ section.id }}-padding color-{{ section.settings.color_scheme }}">
  <div class="container-xl px-xl-0 px-md-5 px-4">
    <div class="sk-numbered-list__wrapper">
      <div class="sk-numbered-list__content-section">
        {%- for block in section.blocks -%}
          {%- case block.type -%}
            {%- when 'numbered_item' -%}
              <div class="sk-numbered-list__item" {{ block.shopify_attributes }}>
                <div class="sk-numbered-list__number">
                  {{ block.settings.number | escape }}
                </div>
                <div class="sk-numbered-list__text">
                  {%- if block.settings.text != blank -%}
                    {{ block.settings.text }}
                  {%- endif -%}
                </div>
              </div>
          {%- endcase -%}
        {%- endfor -%}
      </div>

      <div class="sk-numbered-list__image-section">
        {%- if section.settings.featured_image != blank -%}
          <div class="sk-numbered-list__image-wrapper">
            <img
              srcset="
                {%- if section.settings.featured_image.width >= 400 -%}{{ section.settings.featured_image | image_url: width: 400 }} 400w,{%- endif -%}
                {%- if section.settings.featured_image.width >= 600 -%}{{ section.settings.featured_image | image_url: width: 600 }} 600w,{%- endif -%}
                {%- if section.settings.featured_image.width >= 800 -%}{{ section.settings.featured_image | image_url: width: 800 }} 800w,{%- endif -%}
                {{ section.settings.featured_image | image_url }} {{ section.settings.featured_image.width }}w
              "
              src="{{ section.settings.featured_image | image_url: width: 600 }}"
              sizes="600px"
              alt="{{ section.settings.featured_image.alt | escape }}"
              class="sk-numbered-list__image"
              width="{{ section.settings.featured_image.width }}"
              height="{{ section.settings.featured_image.height }}"
              loading="lazy"
            >
          </div>
        {%- endif -%}
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Numbered List",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "image_picker",
      "id": "featured_image",
      "label": "Featured Image"
    },
    {
      "type": "select",
      "id": "color_scheme",
      "options": [
        {
          "value": "accent-1",
          "label": "t:sections.all.colors.accent_1.label"
        },
        {
          "value": "accent-2",
          "label": "t:sections.all.colors.accent_2.label"
        },
        {
          "value": "background-1",
          "label": "t:sections.all.colors.background_1.label"
        },
        {
          "value": "background-2",
          "label": "t:sections.all.colors.background_2.label"
        },
        {
          "value": "inverse",
          "label": "t:sections.all.colors.inverse.label"
        }
      ],
      "default": "background-1",
      "label": "t:sections.all.colors.label"
    },
    {
      "type": "header",
      "content": "Section Padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Top",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 80
    }
  ],
  "blocks": [
    {
      "type": "numbered_item",
      "name": "Numbered Item",
      "settings": [
        {
          "type": "text",
          "id": "number",
          "label": "Number",
          "default": "01"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "Text Content",
          "default": "<p>Diam facilisi insolens per cu, eos malorum voluptaria ei. Esse concludaturque usu id, et facete quaeque consequat vis. Quo ex ocurreret quaerendum.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Numbered List",
      "blocks": [
        {
          "type": "numbered_item",
          "settings": {
            "number": "01",
            "text": "<p>Diam facilisi insolens per cu, eos malorum voluptaria ei. Esse concludaturque usu id, et facete quaeque consequat vis. Quo ex ocurreret quaerendum.</p>"
          }
        },
        {
          "type": "numbered_item",
          "settings": {
            "number": "02",
            "text": "<p>Diam facilisi insolens per cu, eos malorum voluptaria ei. Esse concludaturque usu id, et facete quaeque consequat vis. Quo ex ocurreret quaerendum.</p>"
          }
        }
      ]
    }
  ]
}
{% endschema %}
